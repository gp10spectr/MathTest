#pragma once

class MathTest {
private:
	//task field
	int count;
	int* user_answers;
	int correct_count;
public:
	//constructor (init with size, init with size and range, init with size and range and operation)
	//deconstructor
	//run()
	//init_test()
	//show_statistics(tab with user answers and real answers)
	//show_question(ask user questions)
};